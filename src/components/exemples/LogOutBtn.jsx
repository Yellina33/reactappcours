const LogOutBtn = ({onClick}) => {
    return(
        <div>
            <button onClick={onClick} className="btn btn-danger border-none px-3">Logout</button>
        </div>

    )
}


export default LogOutBtn