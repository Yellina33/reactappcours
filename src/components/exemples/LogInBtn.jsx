const LogInBtn = ({onClick}) => {
    return(
        <div>
            <button onClick={onClick} className="btn btn-light border-none px-3">Login</button>
        </div>
    )
}

export default LogInBtn